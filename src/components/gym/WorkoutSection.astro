---
interface Props {
	title: string;
}

const { title } = Astro.props;
---

<section
	class="mb-8 p-4 rounded-xl bg-card border border-(--color-border) shadow-sm"
>
	<h3 class="text-xl font-bold mb-4 text-primary">
		{title}
	</h3>
	<div class="flex flex-col gap-4">
		<slot />
	</div>
</section>
